Vamos a crear un controlador y modelo para el recurso plato
(dish, de comida)

* Propiedades del recurso:
     id tipo string
	 name tipo string
	 price float
	 
	 
* Generar el codigo de API necesario en el controlador para responder 
a las siguientes peticiones.
    . Obtener el listado de platos
	. Obtener un plato en base a su id.
	. Obtener un plato en base a su nombre.
	. Actualizar un plato (actualizacion completa)
	. Borrar un plato.

* Crear una lista estática para simular la capa de persistencia.